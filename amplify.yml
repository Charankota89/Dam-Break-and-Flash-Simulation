version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm install
        - chmod +x node_modules/.bin/* || true
    build:
      commands:
        - npx vite build
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'
